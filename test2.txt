programa foreveralone; 
var
	int i, j, p;
	int Arreglo[5], OtroArreglo[10];
	float valor; 

funcion int fact (int j)
var int i;
{
	i = j + (p - j * 2 + j);
	si (j == 1) entonces 
	{ 
	regresa (j);
	}
	sino
	{
	regresa (j * fact(j - 1));
	}
}

funcion void inicia (int y, float a, char b)
var int x;
{
	x = 0;
	mientras (x < 11) haz
	{
	%%Arreglo[x] = y * x;
	x = x+1;
	}
}

principal()
{
	lee(p); j = p * 2;
	inicia (p * j - 5, 2.2, 'a');
	desde i = 0 hasta 9 hacer
		{ 
		p = fact(p);
		%%Arreglo[i] = Arreglo[i] * fact(Arreglo[i] - p);
		}
	desde i = 0 hasta 9 hacer
		{ 
		p = j;
		%%OtroArreglo[i] = Arreglo[i] - p;
		}
	mientras (i < 10) haz
	{
		%%escribe("Otros datos", OtroArreglo[i], p, i + OtroArreglo[i]);
		escribe("Otros datos", p, i);
		i = i + 1;
	}
}
